 const fastcsv = require('fast-csv');

 function csv2ObjectPromiseMaker(headers2Parse,filePath){
 	let csvReadResult = {
 		filePath : '',
 		data :[],
 		version :''

 	};

 	let csv2ObjectPromise = new Promise(){
 		
 	}
 }